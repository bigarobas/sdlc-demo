// Structural evals: zero tokens, zero network. Asserts that the artifacts our
// skills generate keep their required shape, so a change to .claude/** cannot
// silently degrade the SDLC itself. See docs/sdlc/0000-bootstrap/spec.md section 5.
console.log('evals: no cases yet (shell) — passing');
process.exit(0);
